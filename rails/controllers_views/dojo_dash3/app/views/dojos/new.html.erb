<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Create a New Dojo</title>
</head>
<body>
    <!-- <% if flash[:errors] %>
        <% flash[:errors].each do |e|%>
            <p><%= e %></p>
        <% end %>
    <% end %> -->
    <!-- refactoring flashes: -->
    <% if @dojo.errors.any? %>
        <% @dojo.errors.full_messages.each do |e|%>
            <p><%= e %></p>
        <% end %>
    <% end %>
<!-- Keep in mind, these are not flash messages. Benefit of doing this is so that if there are errors, user is redirected to the same page becuase of errors and they will lose everything that was typed. With this the user is able to still receive flash messages and not have the information deleted when redirected to the same page -->

    <h1>New Dojo</h1>
    <form action="/dojos" method="post">
        <input type="hidden" name="authenticity_token" value="<%=form_authenticity_token%>">
        <p>
            <label for="branch">Branch</label>
            <input type="text" name="dojo[branch]" value="<%=@dojo.branch%>">
        </p>
        <p>
            <label for="street">Street</label>
            <input type="text" name='dojo[street]' value = "<%=@dojo.street%>">
        </p>
        <p>
            <label for="city">City</label>
            <input type="text" name="dojo[city]" value = "<%=@dojo.city%>">
        </p>
        <p><input type="submit" value="Create"></p>
    </form>
    <h3><p><a href="/dojos">Home</a></p></h3>
</body>
</html>